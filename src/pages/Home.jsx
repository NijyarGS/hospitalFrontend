import Cases from "../components/Cases";
import CasesHeader from "../components/Cases/CassesHeader";
export default function Home() {
  return (
    <div className="h-100">
      <CasesHeader />
      <Cases />
    </div>
  );
}

// -- Filters Design --
// -- Filters apply to cases --
// -- Casses Fetch  --
// -- Casses all condition rendering --
// -- Casses Page Redisign --
// Translate to arabic

// Finish status Filter
// Finish Doctor id filteration and data structure and convert to drop down
